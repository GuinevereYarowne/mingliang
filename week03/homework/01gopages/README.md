# 并发网页图片统计工具

## 功能说明
并发爬取指定范围（第8~48个）的网页，统计图片总数（不去重/去重）并输出任务耗时。
且最大并发<=5

## 依赖安装
需要安装HTML解析包：
go get golang.org/x/net/html

## 相关附加小说明
generateUrls()：只负责生成目标 URL 列表
fetchImageUrls()：只负责爬取单个网页 + 解析图片链接
main()：就调用函数，然后计个时，打印一下

判断某图片链接是否存在可以用map的key唯一性来进行存储已出现的图片链接

加个defer，这样函数退出一定会执行这个，来释放相关资源

